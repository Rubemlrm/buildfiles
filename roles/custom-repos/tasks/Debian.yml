- name: Add Custom repos for Debian Based
  become: true
  become_user: root
  become_method: sudo
  block:
    - name: Add gpg keys
      apt_key:
        url: '{{ item }}'
        state: 'present'
      with_items:
        - 'https://download.docker.com/linux/debian/gpg'
    - name: Add repos
      apt_repository:
        repo: '{{ item }}'
        state: 'present'
      with_items:
        - 'deb [arch=amd64] https://download.docker.com/linux/debian {{debian_current}} stable'
    - name: update repos
      apt:
        name: '*'
        state: latest
        update_cache: yes
        force_apt_get: yes
    - name: add repos with curl
      raw: '{{ item }}'
      with_items:
        - curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
